fechar o designer

completar os documentos

posts(textos, vídeos, demonstracões)

fechar vídeo

desenvolvimento:

    fechar confirmação de cadastro
    exibir número de seguidores e de pessoas que o usuário segue no perfil do usuário
    exibir fotos de alguns seguidores e pessoas que o usuário segue no perfil do usuário
    exibir lista de todos os seguidores
    exibir lista de todas as pessoas que o usuário segue
    addtoany na página de exibição da oferta
