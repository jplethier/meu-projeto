<% if object.errors.any? %>
  <div id="error_explanation">
    <h2>Verifique o(s) problema(s) abaixo antes de prosseguir:</h2><br/>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
