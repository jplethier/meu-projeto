<%= form_for([@oferta, @comment]) do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <div class="field">
        <%= f.label "Novo Comentário" %><br/>
        <%= f.text_area :content %>
    </div>
    <div class="actions">
        <%= f.submit "Confirmar" %>
    </div>
<% end %>
