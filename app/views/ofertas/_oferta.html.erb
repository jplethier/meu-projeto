<fieldset>
<legend><%= link_to oferta.title, oferta %> - R$ <%= oferta.price_mask %></legend>
    <%= oferta.description %>
    <br/><br/>
    Oferta criada em <%= oferta.created_at.strftime("%d de %B de %Y") %>

</fieldset>
